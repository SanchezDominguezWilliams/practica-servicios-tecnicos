CREATE DATA BASE IF NOT EXISTS servicios_tecnicos;
USE servicios_ecnicos;


CREATE TABLE IT NOT EXISTS usuario (
    id INT AUTO_INCREMENT PRIMARY KEY
    nombre VARCHAR(100) NOT NULL
    correo VARCHAR(100) UNIQUE NOT NULL
    telefono VARCHAR(15)
);
